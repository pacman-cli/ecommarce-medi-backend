package com.example.ecommerce.search.enums;

/**
 * Sort ordering choices for catalogue product search queries.
 */
public enum SearchSortOption {
    RELEVANCE,
    PRICE_ASC,
    PRICE_DESC,
    NEWEST,
    BEST_SELLING,
    HIGHEST_RATED
}
